import styled from '@emotion/styled'

export const StickySearchFormStyled = styled.div`
  position: sticky;
  top: 0;
  z-index: var(--z-index-sticky);
  background-color: #0f092075;
  text-align: center;
  padding: 0.5rem 0;
  backdrop-filter: blur(20px);
`
